export default function(state, action) {
    switch (action.type) {
        case "SEND_MESSAGE":
            return "";
        case "RECEIVE_MESSAGE":
            return "";
        default:
    }
    return "";
}
